[package]
name = "fixbolt-sbe"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false
description = "SBE 1.0 decode in place over schema tables, no allocation, no_std"

# No [dependencies] and no [dev-dependencies], on purpose (ADR-0081 decision 1:
# zero runtime dependencies). `scripts/check-no-optional-deps.sh` asserts that
# nothing from outside this workspace ever enters this crate's normal graph.

[lints]
workspace = true
