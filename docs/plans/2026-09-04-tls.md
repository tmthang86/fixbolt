# TLS: `rustls` bắt tay, kTLS đường nóng, và một gate nói mode nào đang chạy

> **Loại:** Plan · **Ngày:** 2026-09-04 · **Trạng thái:** Chờ duyệt *(xác minh lại 2026-09-09)*
> **Phạm vi:** `STATUS.md` item 45, cuối đợt B. Chạm `engine` (`transport` — implementation thứ
> hai sau feature `tls`), `settings`, `tools/w2w`, `scripts/`, docs. **Không chạm** `codec`,
> `session`. Quyết định đã có: ADR-0005 (Accepted), ADR-0018 (kTLS trên socket thường: **được**,
> bốn điều kiện).
>
> **Draft viết 2026-09-04.** Đây là plan **lớn nhất** trong bộ draft và dựa trên một spike, không
> trên code đã merge. Khi đến lượt: chạy lại `scripts/check-ktls-on-a-plain-socket.sh` trên
> kernel lúc đó, đọc lại ba câu hỏi mở của ADR-0005, kiểm phiên bản `ktls-core`/`rustls`, rồi
> mới *Chờ duyệt*. **Thời lượng dự kiến:** 3–5 ngày, **cần Linux** cho mọi bước từ 2 trở đi.
>
> **Sửa 1 — 2026-09-09, xác minh lại trước khi duyệt, theo đúng luật của item 45.** Bốn việc
> draft tự đặt ra đều đã làm, và kết quả ở mục *Những gì đã biết chắc* bên dưới. **Một phát hiện
> đổi hình dạng của bước 2 và bước 3**, và nó là loại mà việc soát lại từng dòng của bảng không
> bao giờ tìm ra — vì bảng chưa từng có dòng đó: `PendingSet` là generic trên **đúng một** kiểu
> transport, nên "thêm một giai đoạn `Handshaking`" không có chỗ để đứng. Xem *Sửa 1* ở cuối.

## Bối cảnh

Không có TLS thì fixbolt không nối được với đa số venue ngoài colo. ADR-0005 quyết định hình
dạng (bắt tay userspace, kTLS ở trạng thái ổn định, fallback userspace **nói rõ là rời hot
path**) và ADR-0018 đã trả lời câu hỏi load-bearing: `ktls-core` chạy được trên socket
non-blocking thường, không async runtime, với bốn điều kiện đã đo. Cái còn thiếu là **code**,
và một gate cho ADR-0005 câu hỏi mở 3: *cái gì khẳng định mode nào đang chạy* — vì một session
âm thầm rơi xuống userspace là một số latency công bố sai.

## Những gì đã biết chắc (2026-09-04, **xác minh lại 2026-09-09**)

Dòng in đậm là dòng đã đổi ở lần xác minh. Dòng còn lại đã kiểm và giữ nguyên.

| Sự thật | Nguồn |
|---|---|
| `Transport` trait: `recv`, `send`, `POLLABLE`/`source()`; `TcpTransport` set `nodelay` | **`crates/engine/src/transport.rs:132–225`** (trait bắt đầu ở 132, không phải 145). `git log` cho thấy file **không đổi dòng nào** kể từ 2026-09-04 |
| **`PendingSet<T, R, PRE>` giữ `Pending<T, PRE> { transport: T, rx: Framer, … }` — một kiểu transport cho cả set.** `admit(transport: T)` vào cửa, `take(i) -> Option<Pending<T, PRE>>` ra cửa | `crates/engine/src/presession.rs:623-687, 830`. **Dòng này chưa từng có trong bảng, và nó đổi bước 2** |
| **Handshake vì thế phải nằm *bên trong* `TlsTransport::recv`/`send`**, trả `Io::WouldBlock` cho tới khi xong — `PendingSet::turn` đã lái đúng như vậy sẵn | suy ra từ dòng trên; không cần giai đoạn mới nào |
| **`ktls-core` vẫn là 0.0.5** (`cargo search`, 2026-09-09); `rustls` 0.23.43 vẫn nằm trên nhánh ổn định 0.23 — 0.24.0-dev.1 là pre-release | `spikes/ktls/Cargo.toml`, `cargo search` |
| **`scripts/check-ktls-on-a-plain-socket.sh` XANH trên kernel hôm nay**, `7.0.0-31-generic`: spike **15 pass / 0 fail**, `TlsTxSw 0 → 2`, `TlsRxSw 0 → 2`, nhánh GREEN chỉ `recvfrom`/`sendto`, nhánh RED bị `poll` làm đỏ đúng như thiết kế | `[measured 2026-09-09]`, chạy tại chỗ |
| **12 hàm `serve*` / `connect_and_serve*`, không phải con số draft ngầm giả định**, và **mọi hàm đã nhận thêm tham số cuối `observe::Handles`** (ADR-0054, sau khi draft này được viết) | `crates/engine/src/lib.rs:1578…2061`, `crates/engine/src/shard.rs:440, 469` |
| **`settings` đã đi từ 11 lên 26 key, file cấu hình phân vai, có `Registry`** (ADR-0055). `docs/CONFIGURATION.md` giờ kiểm hai chiều, nên bước 6 phải hạ cánh vào cấu trúc đó chứ không phải một danh sách rời | `crates/engine/src/settings.rs`, ADR-0055 |
| **Câu hỏi mở của ADR-0005 còn *bốn*, không phải ba**: 2 (kernel/suite tối thiểu), 3 (gate mode), **4 (mTLS)**, **5 (SNI / nhiều chứng chỉ)**, 6 (key update) | `docs/decisions/ADR-0005-*.md:143-160` |
| Bốn điều kiện kTLS: mọi lỗi read qua `Context::handle_io_error`; transport không đọc socket ngoài offload; bàn giao với buffer rỗng; `setup_ulp` cần `ESTABLISHED` | ADR-0018, `docs/reference/ktls-on-a-plain-socket.md` |
| Spike: `spikes/ktls`, **ngoài workspace** vì kéo `rustls` + `ring`; `scripts/check-ktls-on-a-plain-socket.sh` có nhánh đỏ | `Cargo.toml` workspace `exclude`, `DESIGN.md` §6 |
| Bắt tay được phép cấp phát — carve-out có tên, giới hạn ở bắt tay | ADR-0005 quyết định 1 |
| Câu hỏi mở còn lại: kernel/cipher suite tối thiểu (2); gate cho mode (3); TLS 1.3 key update (6) | ADR-0005 |
| `--no-default-features` phải build không có crypto; CI chạy trên máy không cài gì | bất biến 6, `check-no-optional-deps.sh` |
| Pre-session giữ socket tới `Logon` trên acceptor thread — thread **được phép** block | ADR-0020 |
| `hft` cấm blocking trên engine thread; bắt tay TLS là blocking-shaped | bất biến 4 |
| Hàng §8 TLS trống, cố ý, tới khi `w2w` chạy ba cách trên cùng máy | `DESIGN.md` §8, ADR-0005 quyết định 5 |

## Cách làm — hình dạng dự kiến

1. **Feature `tls`** trong `engine`, tắt mặc định: `rustls` + `ktls-core` là `optional`; `mod tls`
   sau `#[cfg(feature = "tls")]`; `build.rs` không có. `check-no-optional-deps.sh` là gate.
2. **Bắt tay nằm bên trong `TlsTransport`, không phải một giai đoạn mới của `PendingSet`.**
   `[sửa 2026-09-09]` Draft cũ định thêm giai đoạn `Handshaking` trước `AwaitingLogon`. Không có
   chỗ để nó đứng: `PendingSet<T, …>` giữ đúng một kiểu transport từ `admit` tới `take`, nên một
   giai đoạn làm **đổi kiểu** của socket là không biểu diễn được.
   Hình dạng đúng lại **đơn giản hơn**: `TlsTransport::recv`/`send` tự chạy `rustls` và trả
   `Io::WouldBlock` cho tới khi bắt tay xong. `PendingSet::turn` vốn đã quay vòng qua `WouldBlock`
   và đã có deadline `Limits.logon_ms` — **không sửa một dòng nào của `presession.rs`**, và
   `PendingSet` chỉ cần được dựng với `T = TlsTransport`. Vẫn ở acceptor thread, vẫn ngoài engine
   thread. Initiator: cùng cách, trong `connect_and_serve`, trước khi engine nhận socket.
3. **Bàn giao**: sau bắt tay, `ktls-core` `setup_ulp` + đẩy key xuống kernel; từ đó socket là
   `TlsTransport::Kernel(TcpTransport)` — **cùng `recv`/`send`**, D8 spin không đổi, parse tại
   chỗ không đổi. Nếu kernel từ chối (suite, phiên bản): `TlsTransport::Userspace(rustls Stream)`
   — copy một lần mỗi chiều, cấp phát, và **được ghi nhận**.
4. **Gate mode (ADR-0005 câu 3):** `Transport::mode() -> TlsMode { Plain, Kernel, Userspace }`;
   `SessionSnapshot` mang nó; `EventKind::TlsFellBackToUserspace`; `w2w` in mode và
   `check-no-kernel-sleep.sh` chạy thêm một lần với `--tls ktls` và **thất bại nếu mode in ra không
   phải `Kernel`**. Một deployment `hft` có thể từ chối userspace bằng
   `Settings` `TlsRequireKernel=Y`.
5. **Ba lần `w2w` trên cùng máy §9**: plain, kTLS, userspace — điền hàng §8. Không có số này thì
   plan chưa đóng (ADR-0005 quyết định 5).
6. Key file: `SocketUseSSL=Y` (tên QuickFIX), `TlsCertificate`, `TlsPrivateKey`, `TlsCaFile`,
   `TlsRequireKernel`. `[sửa 2026-09-09]` Năm key này vào **`Key` enum của `settings.rs`** — hiện
   là 26 key, có phân vai acceptor/initiator (ADR-0055) — và mỗi key một hàng trong
   `docs/CONFIGURATION.md`, **kiểm hai chiều** như 26 key kia. Không phải một danh sách rời.

## Bất biến bị đụng tới

| Điều | Ảnh hưởng | Giữ bằng cách nào |
|---|---|---|
| **1 — không cấp phát** | bắt tay cấp phát (carve-out); userspace fallback cấp phát **và rời guarantee** | `benches/alloc.rs` case `tls-ktls-busy` = 0 trên Linux có kTLS; case userspace **không** được assert 0 mà assert `mode == Userspace` |
| **4 — không ngủ trong kernel** | bắt tay là blocking-shaped | ở acceptor thread; `check-no-kernel-sleep.sh` với kTLS: chỉ `recvfrom`/`sendto` (như spike) |
| **6 — feature gate** | ba dep mới | `optional`, `#[cfg]` trên `mod`, CI `--no-default-features` + `check-no-optional-deps.sh` |
| 7 — không unwrap | `rustls` error path | clippy |
| 10 — số có máy | hàng §8 | ba arm cùng máy, `check-machine.sh` `pass` |

## Chia việc (dự kiến)

| Bước | Kết quả |
|---|---|
| 1 | Feature + dep + `mod tls` rỗng; gate no-optional-deps xanh; **có thể trên macOS** |
| 2 | Bắt tay trong pre-session (acceptor) qua `Loopback`/`rustls` in-process; test `crates/engine/tests/tls.rs` với chứng chỉ tự ký sinh trong test (**`rcgen` chỉ trong dev-deps**) |
| 3 | Bàn giao kTLS; `TlsMode`; test **chỉ Linux** có kTLS, skip exit 2 ở nơi khác như script spike |
| 4 | Userspace fallback + event + `TlsRequireKernel` |
| 5 | Initiator |
| 6 | `w2w --tls`; script kernel-sleep arm mới; ba số §8; ADR-0005 câu 2 và 6 trả lời bằng đo (một kernel, một suite, một key update ép bằng `rustls` API) |
| 7 | Docs: `DESIGN.md` D11 *As built* + §8 + §9 hàng TLS; `CONFIGURATION.md`; `GUIDE.md` mục TLS; `best-practices-hft.md` (kTLS bắt buộc, nêu mode); `CHANGELOG.md`; ADR-0005 câu hỏi mở đóng bằng ghi chú ngày, không sửa quyết định |

## Bẫy đã lường trước

| Bẫy | Test canh |
|---|---|
| Bắt tay lọt lên engine thread (initiator dễ nhất) | `check-no-kernel-sleep.sh` arm TLS: bất kỳ `read`/`write` blocking trước `Logon` trên tid engine là đỏ |
| kTLS "thành công" nhưng thực ra userspace | gate mode + `strace` chỉ `recvfrom`/`sendto` |
| Bàn giao với byte còn trong buffer `rustls` → mất đầu message đầu tiên | điều kiện 3 của ADR-0018; test gửi `Logon` **ngay** sau `Finished` |
| TLS 1.3 key update giữa phiên | ADR-0005 câu 6: test ép key update |
| `rustls` negotiate suite kernel không có → fallback im lặng | `TlsRequireKernel` + event |
| Số §8 từ máy không kTLS | skip là exit 2, không phải pass |

## Ngoài phạm vi

mTLS policy phức tạp (CRL/OCSP), TLS cho `shard` (sau khi một shard chạy), TLS trên macOS như
đường nóng (là userspace, và docs nói vậy).

`[thêm 2026-09-09]` **Hai câu hỏi mở của ADR-0005 mà draft cũ không nhắc, nay nói rõ đứng ở đâu:**

- **Câu 4 — mTLS có thuộc phase 1 không.** Trong phạm vi ở mức *cơ chế*: `TlsCaFile` cộng
  `rustls` client-auth là một cờ, và bước 4 dựng nó. **Ngoài** phạm vi ở mức *chính sách* — ánh
  xạ chứng chỉ sang `SenderCompID`, thu hồi, xoay vòng. Cái đó là ADR riêng.
- **Câu 5 — SNI và nhiều chứng chỉ trên một listener.** **Ngoài phạm vi.** Một chứng chỉ cho một
  listener; một acceptor cần nhiều thì chạy nhiều listener. Ghi vào `GUIDE.md` như một ràng buộc
  người dùng phải biết, chứ không để tự phát hiện.

## Sửa 1 — 2026-09-09, xác minh lại trước khi duyệt

Bốn việc draft tự đặt điều kiện, kết quả:

1. **`scripts/check-ktls-on-a-plain-socket.sh` trên kernel hôm nay** — `7.0.0-31-generic`,
   `[measured 2026-09-09]` **spike 15 pass / 0 fail**; `TlsTxSw 0 → 2` và `TlsRxSw 0 → 2` trong
   `/proc/net/tls_stat`; `wire-is-a-tls-record` và `wire-carries-no-plaintext` đều xanh; hai nhánh
   đảo ngược (`reversal-breaks-it`, `hand-draining-desyncs-the-kernel`) đều đỏ đúng chỗ; nhánh
   GREEN chỉ có `3705 recvfrom` + `1000 sendto`, nhánh RED bị `1000 poll` làm đỏ. **Điều kiện
   load-bearing của ADR-0018 vẫn đúng trên kernel này.**
2. **Phiên bản** — `ktls-core` vẫn 0.0.5, `rustls` 0.23.43 vẫn trên nhánh ổn định. Không đổi gì.
3. **Ba câu hỏi mở của ADR-0005** — thực ra là **bốn**. Câu 4 và 5 nay có chỗ đứng, ở mục *Ngoài
   phạm vi*.
4. **Bảng *Những gì đã biết chắc*** — một dòng sai số dòng, và **ba dòng mới** mà bảng chưa từng
   có.

**Và đây là lần thứ sáu luật xác minh lại của item 45 kiếm được chỗ của nó, theo đúng kiểu đã đau
một lần rồi.** Phát hiện đắt nhất — `PendingSet` chỉ mang một kiểu transport, nên bước 2 của draft
không dựng được như viết — **không đến từ việc soát lại các dòng trong bảng**. Mọi dòng trong bảng
cũ đều đúng. Nó đến từ việc đọc `presession.rs`, một file mà bảng không có dòng nào trỏ tới.
Giống hệt hình dạng của `[measured 2026-09-09]` ở plan 3: soát lại các sự thật một plan đã liệt kê
không tìm ra sự thật nó chưa bao giờ liệt kê. **Lần này kết quả là tin tốt** — hình dạng đúng đơn
giản hơn hình dạng đã vẽ, và `presession.rs` không phải sửa dòng nào.

`[to testing-skills]` — dạng tổng quát: *một bản kiểm tra lại chỉ soi được những gì bản gốc đã kê
ra; thứ nó không kê thì bao nhiêu vòng soát cũng không thấy. Muốn thấy phải đọc thứ khác với cái
danh sách.*

## Nhật ký giao hàng

*(chờ duyệt — chưa bắt đầu. Xác minh lại xong 2026-09-09, xem Sửa 1.)*
